# T-203 合并到 dev

- **编号**：036
- **时间**：2026-05-07 01:46
- **阶段**：合并
- **涉及角色**：merger

## 发生了什么

T-203（Bug 4 planner 热重建 + main.js IPC 联动）squash merge 到 dev。

squash commit hash：025e69c（dev HEAD）

源分支已清理：
- 远程 fix/T203/planner-mode-hot-reload 已删除
- 本地 worktrees/T203/ 已清理

## 修复内容

### app/breaksPlanner.js

```javascript
setSchedulingMode(mode) {
  const oldMode = this.schedulingMode
  this.schedulingMode = mode
  this.emit('schedulingModeChanged', { mode, oldMode })
}
```

emit 契约：IF-1（`schedulingModeChanged` 事件，carrying mode + oldMode）

### app/main.js

两部分修改：
1. **save-setting handler 分支**：检测 schedulingMode 变更
2. **schedulingModeChanged listener**：更新托盘 + 日志

```javascript
ipcMain.handle('save-setting', (event, setting, value) => {
  if (setting === 'schedulingMode') {
    planner.setSchedulingMode(value)
  }
})

planner.on('schedulingModeChanged', ({ mode, oldMode }) => {
  updateTray()
  log.info(`Stretchly: mode ${oldMode} → ${mode}`)
})
```

IF-2 契约（IPC handler + planner listener 联动）

### test/breaksPlanner.js

5 个 ATDD 用例验证 emit 不变量：
1. 正常转换（mode A → B）
2. 同值幂等（mode A → A）
3. 边界条件（null/undefined）
4. 并发安全（连续转换）
5. 事件参数完整性（mode + oldMode 都存在）

### test/mainSaveSettingHandler.js

2 个静态断言：
1. save-setting 的 schedulingMode 分支存在
2. schedulingModeChanged listener 已注册

### 证据材料

文件：`docs/dev-log/screenshots/T203-{app-log,verification}-evidence.txt`

**AC 2.6（5 不变量）证据**：运行时日志 + 5 用例通过
**AC 2.7（tooltip 文案）证据**：app/main.js updateTray 调用路径验证，commit b51db2e

（三段式证据方案：文本日志 + 静态代码推理 + 运行时验证）

## AC 验收

- AC 2.1 ✓（setSchedulingMode 函数存在）
- AC 2.2 ✓（emit schedulingModeChanged）
- AC 2.3 ✓（event.mode 和 event.oldMode 参数）
- AC 2.4 ✓（save-setting handler 分支）
- AC 2.5 ✓（schedulingModeChanged listener 注册）
- AC 2.6 ✓（5 不变量 + ATDD 覆盖）
- AC 2.7 ✓（tooltip 差异化 + 静态分析）

## 质量证据

- CR 审查：APPROVE，无返工项
- Tech-QA：512/512 PASS，所有 AC 通过，lint PASS
- 证据形式：文本+日志+静态分析三段式（agent shell 无 display 约束下的方案）
- 关键 commit：b51db2e（AC 2.7 验证）

## Batch 1 对标

| 任务 | 修复 | AC 数 | 证据形式 | 合并 commit |
|------|------|-------|---------|-----------|
| T-201 | CSS 选择器 | 3 | 视觉截图 | f8bd31f |
| T-202 | i18next 参数 | 5 | 视觉截图 | a0d05e9 |
| T-203 | planner 热重建 | 7 | 文本+日志+静态分析 | 025e69c |

## 后续影响

Batch 1 完成（T-201, T-202, T-203）。
Batch 2 可启动：T-204（Bug 2，P1）、T-205（Bug 3，P2）。
