# T004/T005 合并到 dev

**时间**：2026-04-24 18:50

## 任务信息

- **任务**：T004 BreaksPlanner quota 模式 + T005 测试桩
- **分支**：feat/T004/planner-quota-mode → dev（squash merge）
- **Merge commit**：3b74c67

## 测试状态

- **T004/T005 测试**：41/41 planner pass
- **全量测试**：368/368 pass

## CR 状态

- **CR 结果**：1 轮通过
- **REVIEW 可选建议**（不阻塞合并）：
  1. freeze/unfreeze 顺序一致性（_onIdleFinished 等 vs onPowerResume）
  2. freeze() 公共 API reason='manual' fallback 是否补入 IF-1 枚举
  3. green 档 breakNumber 行为在 PLANNING §4 文档注记

## 影响范围

- **修改文件**：
  - app/breaksPlanner.js：321 → 695 行（+374 行）
  - test/breaksPlanner.js：新创建（726 行）
- **总变更**：1154 insertions, 65 deletions

## 功能总结

- New schedulingMode 'classic' | 'quota' field; classic path zero-regression
- QuotaManager instance + deadlineScheduler for quota mode
- Tier dispatch in nextBreak(): green → classic cadence, yellow/orange → startSoftReminder event, red → full-screen break; dual-red prefers long
- Soft reminder action routing: takeNow / postpone / ignore / autoClose (autoClose does not deduct quota)
- Pause/resume chain freezes QuotaManager (idle / dnd / appExclusion) with reason stacking
- Long-break hard deadline via dedicated scheduler (longBreakDeadlineTick); harassment mode reschedules at harassmentRepeat intervals
- quotaChanged event carries {miniQuota, longQuota, miniTier, longTier} for tray refresh
- setSchedulingMode for runtime switching (does not interrupt active break)
- onPowerResume composes unfreeze + correctScheduler

## 解锁任务

- T007（main.js 集成，opus）
- T008（托盘变色）
- T010（命令/快捷键）
- T012（红档扣费）
