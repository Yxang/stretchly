# T007 merged to dev (main.js soft-reminder integration)

- **编号**：013
- **时间**：2026-04-24 19:30
- **涉及角色**：developer-T007, merger-T007

## 发生了什么

T007（main.js 软提醒集成 + Wayland fallback + powerMonitor 钩子）合并到 dev。分支 feat/T007/main-soft-reminder-integration；merge commit 14f47f4。CR 1 轮直通；全量测试 368/368 pass。

## 代码变更

- **5 文件**，300+ 行新增 / 6 行删除
- **app/main.js**：软提醒窗口显示/隐藏 + powerMonitor resume 钩子集成
- **app/utils/statusMessages.js**、**appIcon.js** 等相关模块
- **总计**：300+ 行变更

## 继承代码 Bug 修复（4 项）

1. **悬空 listener**：soft-reminder 关闭时未清理 event listener → fixed
2. **i18n 对象缺失**：statusMessages 中 i18n 未正确初始化 → fixed
3. **双路 postpone 文档化**：soft-reminder postpone 与 break postpone 路径差异已文档化
4. **T011 漏键补全**：i18n quota.* 键漏补 → fixed（与 T011 i18n 兼容）

## CR 状态

- **CR R1**：1 轮通过，无拒绝

## 测试状态

- **全量测试**：368/368 pass

## Follow-up 建议（非阻塞）

2 条软建议已嵌入 commit message，可作为后续 polish 任务：
1. **IF-7 签名对齐**：soft-reminder action 回调签名与 IF-7 接口定义对齐（currently working as-is）
2. **IF-1 freeze reason 补充**：freeze() 公共 API 补 `reason='powerSuspend'` 枚举值（T007 已实现但建议正式加入 IF-1 spec）

## 解锁任务

- T008（托盘 quota 可视化）、T010（命令与快捷键）可并行启动
- Batch 4 feature complete pending T008、T010 开发

## Pattern Note

🔁 **继承代码 bug 修复集中**：与 T006 CR 阶段发现的文档同步问题同源，反映需求变更快速迭代中细节易漏。后续 i18n / API 契约变更应增强集成测试覆盖。
