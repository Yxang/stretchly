# AC 15.7 hotfix Path B γ-locked merged to dev

- **编号**：022
- **时间**：2026-04-24 13:00（迭代全程）
- **阶段**：完整迭代 + 合并
- **涉及角色**：developer-hotfix-ac15.7, tech-qa-hotfix-ac15.7, architect-2, code-reviewer, merger-hotfix-ac15.7, product-lead-2, Coordinator

## 发生了什么

AC 15.7 hotfix（tier 阈值反序约束）经过完整迭代轨，从路径 A β 实现到路径 B γ-locked 实现，最终合并到 dev。commit 8ce7a0a（squash merge，8 commits）。

## 事实时序（12 步）

### 步骤 1-2：spawn + 路径 A β 实现

- Task #13 AC 15.7 hotfix spawn：dev-hotfix-ac15.7 + tech-qa-hotfix-ac15.7
- 路径 A β 实现：per-value bound 策略（绿档回退到默认值 100）
- CR 路径 A：APPROVED WITH COMMENTS（commit 0c4675b "强化"）

### 步骤 3-4：规格第一轮下达 + 冲突发现

- **PL 第一轮规格**（via Coordinator）：标准表 6 行 + 4-step algorithm with supplementary rules
  - 标准表内部不一致：β 版本（per-value bound）vs γ 版本（4-step cascade）混淆
  - 标准表行计算错误：G=30/Y=50/O=20 行（Y > G，反序失效）
  
- **Coordinator brief 转述错误**：
  - 原表：(G=80, Y=20, O=30)
  - 转述笔误：(G=80, Y=19, O=18) ← 下调错位

### 步骤 5：规格冲突升级 → PL Y-lock

- 规格冲突升级后 → Coordinator 发 PL 输出-only X/Y 锁定需求
- **PL 最终锁定（12:56）**：Y = all-or-nothing γ（保留用户 Yellow，级联下调 Orange/Red）
- **F1=(70, 30, 10)** 作为 tech-qa 最终验收标准（γ 框架下的默认值）

### 步骤 6-7：路径 B γ 实现 + tech-qa F1 oscillation

- **路径 B γ 实现**：
  - fb9a3e3：QuotaManager._sanitizeTierThresholds() 级联公式
  - 684cf8c：test 完全重写（8 新用例 + 64 继承）
  
- **Tech-QA F1 oscillation**（规格变更后多轮反复）：
  - 初期：γ（正确）
  - 中期：β（路径 A 遗留）
  - 最终：γ（684cf8c，F1=(70,30,10) 正确定稿）

### 步骤 8-10：Leader rerun + CR + 合并

- **Architect-2 Leader rerun**：73/73 tests pass（canonical 标准，覆盖 tech-qa 自报 72/72 fd2f1e4 笔误）
- **集成快检 PASS**：调用点封装 + 共享文件未触碰 + merge-tree 无冲突
- **CR 路径 B ACCEPT**：11-point 全绿，四者规格对齐（developer / tech-qa / architect / code-reviewer）
- **Merger squash**：8 commits → dev 1 commit（8ce7a0a）

### 步骤 11-12：shutdown + 流程终止

- Merger 完成后三个 per-task agents + stray dev-T008 全 shutdown
- AC 15.7 hotfix pipeline 完结

## 规格演化（β → γ）

**路径 A β**：per-value bound（每个阈值单独回退默认）
```
if (Y >= G) Y ← 100；
if (O >= Y_corrected) O ← 30；
if (R >= O_corrected) R ← 10；
```

**路径 B γ（最终）**：公式级联下调保留用户 Yellow
```
// 保持 Y（用户约束点）
if (Y >= G) Y_corrected ← G - 1；
// 级联调整 O 和 R
if (O >= Y_corrected) {
  O_corrected ← Y_corrected - 1；
  R_corrected ← O_corrected - 1；
}
```

**PL Y-lock（12:56）**：Y = all-or-nothing γ（一旦确定路径 B，所有逻辑遵循 γ）

## CR 路径 A vs 路径 B

| 阶段 | 分支 | Verdict | 理由 |
|------|------|---------|------|
| 路径 A | per-value bound | APPROVED WITH COMMENTS | 算法正确，但规格与 PL 期望不符 |
| 路径 B | 级联下调保留 Y | ACCEPT（11-point 全绿） | 规格与 AC 对齐，算法正确 |

## 合并信息

- **分支**：hotfix/ac15.7-tier-threshold-order
- **Merge 方式**：squash（8 commits → 1）
- **Merge commit**：8ce7a0a
- **Dev HEAD**：8ce7a0a（current）
- **合并时间**：2026-04-24 13:xx

## LESSONS v1.23 候选汇总（12 条）

累计自 dev-log 016-022，共 12 条教训待 Phase 6 Checkpoint 评审：

1. **Worktree lint scope 污染**（dev-log 017）
   - eslint config scope 过宽导致跨 worktree 阻塞

2. **TaskCreate 预录任务必须 owner + blockedBy 预锁**（dev-log 016）
   - per-task agent 自动认领绕过 Leader 分配

3. **路径 B γ Y-locked 规格收敛**（本条目）
   - PL 规格变更需明确 all-or-nothing 锁定，避免 agent 理解偏差

4. **Tech-QA F1 oscillation**（本条目）
   - 规格变更后多轮反复验收，需 Leader rerun canonical 确认

5. **架构师向 per-task agent 发 brief 需先过 Coordinator**（dev-log 019+020）
   - Brief 需 AC/PLANNING 原文引用 + Coordinator 审核 + 备案

6. **Agent 自述绿需 Leader rerun**（本条目）
   - 73/73 vs 72/72 笔误，Leader 独立运行是 canonical 标准

7. **规格多轮变更后字面执行与结论验证必须做一次**（本条目）
   - tech-qa F1 oscillation 反映规格稳定后需全量重验

8. **PL 标准表行 vs 算法自洽检查**（本条目）
   - 手算错误（G=30/Y=50/O=20 反序失效）需标准表验算机制

9. **Per-task agent SHA 汇报精度不足**（本条目）
   - fd2f1e4 笔误暴露需 Leader 复跑确认 canonical commit

10. **Coordinator brief 标准表需手算验算**（本条目）
    - (80,20,30) → (80,20,19) 错写 (80,19,18)

11. **Batch 合并后 architect 必须清点 shutdown sweep per-task agents**（本条目）
    - dev-T008 stray 佐证需显式清点

12. **Per-task agent shutdown 时机 = 该 agent 视角任务结束（非 pipeline 结束）**（本条目）
    - 明确 shutdown 触发点避免 stray agents 累积

## 规格溯源档案（重点节点）

- **PL 消息 1（12:49:19）**：β 初诊（PL 自己撤回，标准表有误）
- **PL 消息 2（12:49:39）**：架构师版 4-step algorithm（无补充规则）
- **PL 消息 2（~12:51）**：我的 resend 版 4-step + 两条补充规则
- **PL Final Call（~12:52:30）**：(70,30,10) 口径确认
- **PL Final Lock（12:56）**：Y = all-or-nothing γ（一字母锁定）

**多轮转发违规**：
- Architect-2 直发 brief 给 dev（违反 LESSONS #5） × 1
- PL 直发规格给 Coordinator（违反 LESSONS #7） × 3

## 里程碑

AC 15.7 hotfix 完整迭代轨记录完整。Path B γ-locked 实现合并到 dev（8ce7a0a）。三个 per-task agents 已 shutdown。12 条 LESSONS v1.23 候选待 Phase 6 Checkpoint 评审。

---

**后续**：PQ 第一轮由 PL 负责（与 hotfix 完成后才最终验证 AC 15.6/15.7）。
