# 架构宪法（Constitution）

## 项目：[项目名称]
## 创建日期：[日期]
## 最后修订：[日期]
## 维护者：架构师

> 本文档定义跨版本持久的架构原则。所有版本的 PLANNING.md 设计决策不得违反本文档。
> 修改流程：架构师提出变更 → Coordinator 审批 → 追溯员记录。

---

## 1. 技术栈约束

不随版本变化的技术选型约束。

- [如："shared/ 模块零 npm 依赖（纯 TypeScript）"]
- [如："所有 ML 推理通过 onnxruntime-react-native（CoreML 后端）"]
- [如："npm install 必须加 --legacy-peer-deps"]

---

## 2. 架构原则

项目的结构性不变量。

- [如："双模型架构：Piano/Guitar → Basic Pitch，Voice → CREPE"]
- [如："实时反馈统一使用 YIN 算法（< 20ms 延迟）"]
- [如："所有 DSP 模块不依赖外部库"]

---

## 3. 开发范围

长期有效的开发方向约束。

- [如："Mobile 端为主要开发目标，Web 端纯维护模式"]
- [如："CLI 仅用于调试和测试，不面向最终用户"]

---

## 4. 跨模块约定

所有模块必须遵守的公共约定。

- [如："所有时间戳使用 ISO 8601 UTC"]
- [如："错误格式统一为 { error: string, code: number }"]
- [如："文件路径使用 expo-file-system/legacy 导入"]

---

## 5. 质量底线

不可降级的质量标准。

- [如："所有公共接口必须有 TypeScript 类型定义"]
- [如："Hermes 运行时无 Buffer 全局变量，使用 btoa() 替代"]

---

## 6. 安全底线

不可妥协的安全约束。

- [如："不在客户端存储 API 密钥或敏感凭证"]
- [如："用户数据不上传到外部服务（除非用户明确授权）"]

---

## 修订记录

| 日期 | 修订内容 | 提出者 | 审批者 |
|------|---------|--------|--------|
| [创建日期] | 初始版本 | 架构师 | Coordinator |
